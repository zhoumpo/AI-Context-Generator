<!-- MainWindow.xaml -->
<Window x:Class="AI_Context_Generator.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Codebase Scanner" Height="450" Width="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox x:Name="DirectoryPathTextBox" Width="600" Margin="0,0,10,0" 
                     Text="{Binding SelectedDirectory, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Browse..." Width="80" Margin="0,0,10,0" Click="BrowseButton_Click"/>
            <Button Content="Scan" Width="80" Click="ScanButton_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Label Content="Exclude patterns (comma-separated):" Margin="0,0,10,0"/>
            <TextBox x:Name="ExtensionsTextBox" Width="400" 
                     Text="bin,obj,packages,.exe,.dll"/>
        </StackPanel>

        <GroupBox Grid.Row="2" Header="Progress">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <ProgressBar x:Name="ScanProgressBar" Height="20" Margin="0,0,0,10"
                             Value="{Binding ProgressValue}" Maximum="100"/>

                <TextBox x:Name="LogTextBox" Grid.Row="1" IsReadOnly="True" 
                         VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"
                         Text="{Binding LogText}"/>
            </Grid>
        </GroupBox>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <TextBlock x:Name="StatusText" Text="Ready" Margin="0,0,10,0" VerticalAlignment="Center"/>
            <Button Content="Open Output File" Width="120" Click="OpenOutputButton_Click" IsEnabled="{Binding IsScanComplete}"/>
        </StackPanel>
    </Grid>
</Window>